unittest is the basic unit testing framework, included in distributions of Python

The main unit of a unit test implementation is TestCase. To create a test case create a class which extends unittest.TestCase. A single test case can do some set-up and clean-up.

